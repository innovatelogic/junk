#include "arr.h"
#include <gtest/gtest.h>

namespace junk
{
    namespace combinations_of_sum_num
    {
        TEST(Array, SumCombinations_of_N)
        {
            SumsSeq obj(4);
        }
    }
}